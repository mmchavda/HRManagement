<body>
  <!--Container -->
  <div class="mx-auto bg-grey-400", style="position: fixed; top: 0; width: 100%; z-index: 1000;">
    <!--Screen-->
    <header class="bg-nav">
      <div class="flex justify-between">
        <div class="p-1 mx-3 inline-flex items-center">
          <%= image_tag '/logo-primary.png', alt: 'HR Management Logo', size: "100x100" %>
        </div>
        <div class="p-1 flex flex-row items-center">
          <div class="flex items-center ml-auto relative">
            <% if user_signed_in? %>
              <div class="relative -ml-2" data-controller="dropdown">
                <button 
                  class="flex items-center space-x-2 text-white hover:text-gray-300 focus:outline-none" 
                  data-action="click->dropdown#toggle"
                  type="button"
                  data-turbo="false">
                  <span class="text-white p-2 no-underline hidden md:block lg:block">
                    <i class="fas fa-user mr-1"></i>
                    <%= current_user.email.split('@').first.capitalize %>
                  </span>
                  <i class="fas fa-caret-down text-white", style="margin-right:20px;"></i>
                </button>

                <!-- Profile Dropdown -->

                <div data-dropdown-target="menu" class="hidden absolute right-0 mt-2 w-48 rounded shadow-md bg-white z-50">
                  <ul class="list-reset">
                    <% if current_user.is_active %>
                      <li>
                        <span class="block px-4 py-2 text-sm text-gray-800 font-medium border-b border-gray-200">
                          <i class="fas fa-envelope mr-1 text-gray-800 mr-1"></i>
                          <%= current_user.email %>
                        </span>
                      </li>
                      <li>
                        <%= link_to "/users/#{current_user.id}", class: "no-underline px-4 py-2 block text-black hover:bg-gray-100 text-gray-800" do %>
                          <i class="fas fa-user mr-2 text-gray-800"></i>
                          Profile
                        <% end %>
                      </li>
                      <li>
                        <%= link_to "/users/edit", class: "no-underline px-4 py-2 block text-black hover:bg-gray-100 text-gray-800" do %>
                          <i class="fas fa-key mr-2 text-gray-800"></i>
                          Change Password
                        <% end %>
                      </li>
                      <li><hr class="border-t mx-2 border-gray-200 text-gray-800"></li>
                    <% end %>
                    <li>
                      <%= form_with url: destroy_user_session_path, method: :delete, class: 'block' do |form| %>
                        <button type="submit" class="block w-full text-left px-4 py-2 text-black hover:bg-gray-100 text-gray-800" data-disable-with="Logging out...">
                          <i class="fas fa-sign-out-alt mr-2 text-gray-800"></i>
                          Logout
                        </button>
                      <% end %>
                    </li>
                  </ul>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </header>
  </div>
</body>