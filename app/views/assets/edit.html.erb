<!-- app/views/assets/edit.html.erb -->
<%= form_with(model: @asset, local: true, class: "space-y-6") do |form| %>
  <%= render 'view_edit', asset: @asset, users: @users, asset_assignment: @asset_assignment, page_type: "edit", form: form %>
<% end %>

